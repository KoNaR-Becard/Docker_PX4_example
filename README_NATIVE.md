1. Clone the official PX4 repository to your home folder:
   `git clone https://github.com/PX4/PX4-Autopilot.git ~/PX4-Autopilot`
2. Go there and build it:
   `make px4_sitl`
3. Go to the Docker_PX4_example and there paste:
   `./run_native.sh`
