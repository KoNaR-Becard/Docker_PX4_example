services:
  px4_sim:
    container_name: px4_sim_ros
    network_mode: host
    image: ghcr.io/konar-becard/docker_px4_example:master

    privileged: true
    tty: true
    build:
      dockerfile: Dockerfile
      context: .
    stop_signal: SIGINT
    env_file:
      - env
    environment:
      - ROS_DOMAIN_ID=0
    volumes:
      - ./entrypoint.sh:/entrypoint.sh:rwx
      - /sys:/sys
      - /etc/udev:/etc/udev
      - /run/udev:/run/udev
      - /run/dbus/:/run/dbus
      - /dev:/dev
      - /tmp/.X11-unix/:/tmp/.X11-unix/
      - ./worlds:/app/PX4-Autopilot/Tools/simulation/gz/worlds
      - ./models:/app/PX4-Autopilot/Tools/simulation/gz/models
      - ./airframes:/app/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes

    devices:
      - "/dev/dri/:/dev/dri/"
      - /dev/snd:/dev/snd
      - "/dev/kfd:/dev/kfd"

    group_add:
      - video
      - dialout